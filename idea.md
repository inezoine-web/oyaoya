# oyaoya — 「完全ポンジ仮説」資金残高シミュレーション計画

## 目的

「みんなで大家さん」が販売してきた各ファンドについて、公開情報から募集額・想定利回り・運用期間・償還時期を可能な限り収集し、次の極端な仮説を置いた場合の資金残高を時系列で再構築する。

> **仮説:** 投資対象からの実質的な収益は一切なく、後発ファンドで集めた出資金のみを原資として、既存出資者への分配金と満期償還を支払っていた。

この仮説が事実だと主張するものではない。あくまで公開情報を使った反実仮想の資金繰りシミュレーションとして、

- 利払い（分配）停止時点で理論上いくら現金が残るのか
- 途中で理論残高がマイナスになるか
- 新規募集がどの程度続けば維持可能だったか
- どの時点から資金流入の鈍化に耐えられなくなるか

を確認する。

---

## 基本モデル

最も単純な一本の共通資金プールとして扱う。

月次残高は以下で更新する。

```text
cash[t]
  = cash[t-1]
  + new_investment[t]
  - distributions[t]
  - principal_redemptions[t]
  - other_outflows[t]
  + other_inflows[t]
```

最初のベースラインでは以下を固定する。

```text
investment_income = 0
asset_sale_income = 0
borrowing = 0
other_inflows = 0
other_outflows = 0
```

したがって、完全ポンジ仮説の最小モデルは、

```text
cash[t]
  = cash[t-1]
  + new_investment[t]
  - distributions[t]
  - principal_redemptions[t]
```

となる。

理論残高が途中で 0 未満になる場合、少なくともこの単純モデルだけでは実際の資金繰りを説明できない。その場合は、借入、資産売却、事業収益、募集額の時期差、ロールオーバー等の追加要素が必要になる。

---

## 分配金の計算

各ファンド `i` について、

```text
principal_i = 実際の募集額（不明なら募集予定額）
yield_i     = 年率想定利回り
start_i     = 運用開始日
end_i       = 満期日 / 償還日
```

とする。

月次計算では単純化して、

```text
monthly_distribution_i
  = principal_i * yield_i / 12
```

を基本とする。

ただし実際の商品ごとに分配周期が異なる可能性があるため、データが取れる場合は実際の分配月を使う。

日割りまで再現する必要がある場合は、

```text
distribution
  = principal * annual_yield * active_days / days_in_year
```

へ切り替える。

最初は月次で十分。

---

## 元本償還の扱い

満期時に、原則としてそのファンドの元本全額を出金扱いする。

```text
principal_redemptions[end_month] += principal_i
```

ただし次のケースは別途フラグを持たせる。

- 償還延期
- 運用期間延長
- 他ファンドへの乗り換え
- 再出資 / ロールオーバー
- 一部償還
- 募集未達

特に「償還された元本がそのまま次の商品へ再出資された」場合、実際には一度外部へ現金が出ていない可能性がある。

そこで最終的には、

1. **全額償還モデル**
2. **一定割合ロールオーバーモデル**

の2系統を比較する。

---

## 必要データ

ファンドごとに以下を収集する。

| 項目 | 必須度 | 備考 |
|---|---:|---|
| fund_name | 必須 | シリーズ名・号数 |
| sales_start | 推奨 | 募集開始日 |
| operation_start | 必須 | 運用開始日 |
| operation_end | 必須 | 当初満期日 |
| actual_end | 推奨 | 延長・償還延期を反映 |
| target_amount | 推奨 | 募集予定額 |
| subscribed_amount | 最重要 | 実際の出資額。無ければ予定額 |
| annual_yield | 必須 | 想定利回り |
| distribution_frequency | 推奨 | 月次、隔月など |
| redemption_status | 推奨 | 償還済 / 運用中 / 延期等 |
| source_url | 必須 | 根拠URL |
| source_date | 推奨 | 情報取得日 |
| confidence | 必須 | confirmed / estimated / unknown |
| notes | 任意 | 特記事項 |

CSV例:

```csv
fund_name,operation_start,operation_end,subscribed_amount,annual_yield,confidence
成田18号,2024-05-01,2029-04-30,15368000000,0.07,confirmed
```

※上記はフォーマット例。実データ投入時は必ず公開資料で再確認する。

---

## データ収集対象

可能な限りシリーズ1号から停止直前までを対象にする。

優先順位:

1. 公式商品ページ
2. 公式プレスリリース
3. 行政処分資料
4. 裁判資料
5. 決算公告・財務資料
6. 信用調査会社 / 報道
7. Web Archive 等の過去ページ

公式サイト上から消えた古い商品については、複数ソースでクロスチェックする。

---

## シミュレーション期間

基本期間:

```text
2007年（シリーズ開始）
  ～
最初の大規模な分配停止 / 遅延が確認された時点
```

終了日は固定値ではなくパラメータ化する。

例:

```text
--cutoff 2025-07-31
```

これにより、

- 行政処分日
- 新規募集が実質停止した日
- 解約増加が報じられた日
- 分配遅延公表日

など複数のイベント時点で理論残高を確認できる。

---

## フェーズ1: 最小モデル

まずは割り切って以下だけで計算する。

- 全ファンド満額募集
- 想定利回りどおり分配
- 投資収益 0
- 借入 0
- 資産売却 0
- 解約 0
- 満期時に元本全額償還
- 全ファンドを一本の資金プールとみなす

これで、

```text
理論上、出資金だけで最後まで回せるのか？
```

を最初に見る。

この時点で残高がマイナスになるなら非常に重要。

---

## フェーズ2: 募集時期を現実化

募集額が運用開始日に一括入金されたと仮定すると誤差が大きい可能性がある。

そこで募集期間が分かる商品については、入金を募集期間中に分散する。

単純モデル:

```text
募集期間3か月、募集額120億円
→ 各月40億円流入
```

さらに情報が取れれば実際の募集進捗に近づける。

---

## フェーズ3: 解約・償還延期

行政処分後などに解約が増えたとされる期間についてシナリオ分析する。

例:

```text
scenario A: 解約なし
scenario B: 毎月残高の0.5%解約
scenario C: 毎月残高の1.0%解約
scenario D: 毎月残高の2.0%解約
```

同時に、償還延期された商品については元本流出タイミングを後ろ倒しする。

これにより、

```text
分配停止は「運用損失」よりも
「新規流入減 + 解約増」だけで説明可能か？
```

を検証する。

---

## フェーズ4: ロールオーバー率

満期元本のうち一定割合が次の商品へ再投資されたと仮定する。

```text
rollover_rate = 0.0 ～ 1.0
```

実際の外部流出額:

```text
cash_redemption
  = principal * (1 - rollover_rate)
```

例えば 70% が再投資なら、100億円満期でも現金流出は30億円とする。

ここは結果への影響が非常に大きいので感度分析必須。

---

## フェーズ5: 現実側の資金を追加

完全ポンジ仮説との差分を見るため、あとから現実的な資金源を追加できるようにする。

```text
+ actual_property_income
+ asset_sales
+ bank_loans
+ related_party_loans
- property_acquisitions
- construction_costs
- operating_expenses
- commissions
- advertising
- taxes
```

完全ポンジモデルで必要だった追加資金と、公開財務資料の数字を比較する。

---

## 重要な出力

### 1. 月次キャッシュ残高

```text
month,new_investment,distribution,redemption,cash_balance
```

折れ線グラフで表示。

特に、

- 残高最大値
- 初めて残高がマイナスになる月
- 分配停止時点残高

を強調する。

### 2. 月次必要新規募集額

既存の分配・償還を維持するだけで毎月いくら新規資金が必要だったか。

```text
required_new_money[t]
  = distributions[t]
  + redemptions[t]
  - available_cash[t-1]
```

### 3. ファンド残高総額

その月に運用中とされる投資家元本総額。

```text
outstanding_principal[t]
```

これと月次分配額を並べる。

### 4. 累計値

```text
累計募集額
累計分配額
累計償還額
理論残存現金
```

基本恒等式:

```text
理論残存現金
  = 累計募集額
  - 累計分配額
  - 累計実流出償還額
```

---

## 検算ポイント

公開されている節目の累計値をチェックポイントにする。

例えば、会社側が過去に公表した

```text
累計出資金額
累計配当金額
```

と、収集したファンドデータを積み上げた値を比較する。

差が大きければ、

- 抜けている商品
- 募集額の誤認
- 分配期間の誤認
- 募集未達
- 私募・別シリーズ

などを疑う。

---

## 感度分析

結果を一本の数字に固定しない。

最低でも以下を動かす。

```text
募集達成率       80 / 90 / 100%
ロールオーバー率  0 / 30 / 50 / 70 / 90%
平均解約率       0 / 0.5 / 1 / 2% per month
分配利回り        公称値 / 実績推定値
募集入金タイミング 一括 / 期間均等
```

最終的にはヒートマップ等で、

```text
「この条件なら2025年7月時点で○億円残る」
```

を可視化する。

---

## 面白い指標

### Ponzi Sustainability Ratio (仮称)

```text
PSR[t]
  = new_investment[t]
  / (distributions[t] + cash_redemptions[t])
```

```text
PSR > 1 : 新規流入だけで当月支払を賄える
PSR = 1 : ぎりぎり
PSR < 1 : 過去の現金残高を食い始める
```

これを月次で描くと「どこから崩れ始めるか」が見やすい。

### Runway

その時点で新規募集が完全停止した場合、既存現金だけで何か月分の分配・償還を続けられるか。

```text
runway_months[t]
```

これも行政処分前後で見ると面白い。

---

## 実装イメージ

最初は Python で十分。

```text
data/
  funds.csv
  events.csv
  sources.csv

src/
  load_data.py
  simulator.py
  scenarios.py
  charts.py

output/
  monthly_cashflow.csv
  summary.json
  charts/

idea.md
README.md
```

想定CLI:

```bash
python -m src.simulator \
  --funds data/funds.csv \
  --cutoff 2025-07-31 \
  --rollover-rate 0.0 \
  --redemption-scenario none
```

---

## 最初に作るもの

1. 全ファンド一覧CSV
2. 月次キャッシュフロー計算器
3. 残高グラフ
4. データ欠損チェック
5. ソースURL一覧
6. 複数シナリオ比較

最優先はアルゴリズムではなく **ファンド台帳の再構築**。

計算自体は単純だが、商品ごとの募集額・開始日・満期・延長・実際の償還状況の精度が結果をほぼ決める。

---

## 注意事項

このプロジェクトでは「ポンジであった」と断定しない。

名称としての「完全ポンジ仮説」は、投資収益をゼロと置いた場合の資金繰りを検証するための極端な反実仮想モデルを意味する。

実際の資金使途、不動産収益、借入、関連会社間取引、資産売却等を無視したベースラインなので、現実の財務残高そのものを推定するモデルではない。

むしろ目的は、

> **公開された募集・分配・償還条件だけを使ったとき、理論上どのような資金残高になるのか**

を明らかにし、その結果と現実に起きたイベントの時系列を比較することにある。

---

## ゴール

最終的に、任意の日付について次の問いへ答えられる状態にする。

```text
仮に投資収益が一円もなく、
後発ファンドの出資金だけで分配と償還をしていたなら、
この日の時点で理論上いくら現金が残っていたか？
```

そして逆方向にも、

```text
その資金繰りを成立させるには、
最低でもどれだけの外部収益・借入・ロールオーバーが必要だったか？
```

を計算できるようにする。
